<h3>Здесь отобразятся все посты юзера</h3>

{{!-- <div id="container">
  {{#each everyPost}}
  <div id="{{this.id}}"></div>
    <hr>
    <img src=".{{this.photo}}" alt="Без изображения">
    <a href="/post/{{this.id}}"><p>{{this.title}}</p></a>
    <p>{{this.description}}</p>
    <a href="/post/{{this.id}}/delete"> <button>удалить пост</button></a>
    <img src="{{this.photo}}" alt="">
    <a href="/profile/{{this.id}}/delete"> <button>удалить пост</button></a>
    
  </div>

  {{/each}}
</div> --}}




{{!-- <div class="post">
{{#each everyPost}}
<div class="card" style="width: 25rem;">
  <img src="{{this.photo}}" class="card-img-top" alt="...">
  <div class="card-body">
    <h5 class="card-title">{{this.title}}</h5>
    <p class="card-text">{{this.description}}
    </p>
    <a href="/post/{{this.id}}/delete" class="btn btn-primary">Удалить пост</a>
  </div>
</div>
{{/each}}

</div> --}}

<div class="post">
{{#each everyPost}}
<div class="card" style="width: 25rem;">
  <a href="/post/{{this.id}}">
    {{#if this.photo}}
    <img src="{{this.photo}}" class="card-img-top" alt="photo">
    {{/if}}
    <div class="card-body">
      <h5 class="card-title">{{this.title}}</h5>
      <p class="card-text">{{this.description}}</p>
      <a href="/post/{{this.id}}/delete" class="btn btn-primary">Удалить пост</a>
    </div>
  </a>
</div>
{{/each}}
</div>
