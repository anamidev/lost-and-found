<div id="container">
  {{#each categoryPosts}}
  <div id="{{this.id}}">
    <hr>
    <img src=".{{this.photo}}" alt="Без изображения">
    <a href="/post/{{this.id}}"><p>{{this.title}}</p></a>
    <p>{{this.description}}</p>
  </div>
  {{/each}}
</div>

